# HomeOps V0 UX Context Patch

No API schema changes were required for this UX patch.

Use the same V0 foundation SQL/migration from the prior skeleton patch. This zip is repackaged alongside the updated app so both folders stay in sync for testing.
